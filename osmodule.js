// This is OS which is built in function or liberary Which aldo offer many functions as mention in node js document.

const OS = require('os');

// console.log(OS.freemem());
console.log(OS.homedir());